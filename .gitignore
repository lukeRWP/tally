# Dependencies
node_modules/

# Build
client/build/
client/dist/
server/dist/
# TypeScript incremental cache: machine-specific, rewritten by every tsc run,
# and never a cache hit in CI (which builds from a clean checkout). It was
# tracked until 2026-08-15, so it dirtied the tree constantly and rode along in
# unrelated commits.
*.tsbuildinfo

# Environment
.env
.env.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
docker-data/

# Logs
*.log
logs/

# SSL certs (local dev)
ssl/

# Superpowers
.superpowers/
