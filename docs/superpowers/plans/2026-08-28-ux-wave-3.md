# UX Wave 3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ship the review's remaining findings — #232 scroll restoration, #230 ManualCreate USB path, #231 bulk delete/tag, #229 small-friction batch.

**Architecture:** all client-side (`client/src`); no migration, no server change, no new dependency. Spec: `docs/superpowers/specs/2026-08-28-ux-wave-3-design.md` governs. Bulk ops loop existing single-entity endpoints sequentially with per-entity error isolation (Wave-2 bulk-clear + print-queue precedents). capture.tsx is touched by Tasks 2 and 4 — sequential; RE-ANCHOR by code, not line numbers (Waves 1–2 reshaped it).

**Tech Stack:** React 18 + TS, TanStack Query v5, react-router (useNavigationType), sonner, vitest, Playwright harness (Task 6).

## Global Constraints

- Client gates: `npx tsc --noEmit`, `npx vitest run`, `npm run build` (NO client eslint). Hooks above every early return.
- Tailwind tokens only; match each file's idiom. Ring/highlight wrappers around RuledRow cards must carry `border-b border-[var(--color-rule)] last:border-b-0` (the Wave-2 hairline lesson).
- Bulk loops: sequential `for … await mutateAsync`, continue-on-failure, truthful outcome (`<Verb> N · K failed`), button disabled + progress label while running.
- Line anchors may drift — anchor by quoted code.

---

### Task 1: #232 — Scroll restoration

**Files:** Create `client/src/hooks/use-scroll-restoration.ts`. Modify `client/src/components/layout/root-layout.tsx` (the scroll-to-top effect at ~:232-235 — anchor: the effect watching pathname that sets the main container's scrollTop). Test: `client/src/hooks/use-scroll-restoration.test.tsx`.

**Interfaces:** `useScrollRestoration(ref: RefObject<HTMLElement>)` called once by root-layout with the existing main-container ref; no other consumer.

- [ ] **Step 1: Failing tests:** (a) PUSH to a new path scrolls the container to 0; (b) scrolling to 480 then PUSH away then POP back restores 480 (jsdom: set scrollTop manually, fire the events, mock useNavigationType); (c) the cache survives a remount via sessionStorage; (d) PUSH clears the destination path's stale entry.
- [ ] **Step 2: Implement** the hook: module-level `Map<string, number>` hydrated once from `sessionStorage` (one JSON key, e.g. `tally-scroll-cache`); rAF-throttled scroll listener writes the current pathname's offset (and persists on `pagehide` + route-leave); on pathname change, `useNavigationType() === 'POP'` → `requestAnimationFrame(() => el.scrollTop = cached)` else `el.scrollTop = 0` and delete the entry. Replace root-layout's unconditional reset with the hook.
- [ ] **Step 3:** Gates. Commit: `feat(client): scroll restoration on back-navigation, app-wide (#232)`

### Task 2: #230 — ManualCreate USB path

**Files:** Modify `client/src/pages/capture.tsx` (ManualCreate section — anchor: `mc-barcode` input, the mount-only `nameRef` focus effect, the ManualCreate props list). Test: new `capture.manual.test.tsx`.

**Interfaces:** consumes the page's existing `handleCode`-family lookup helpers (`/api/products/_y_/lookup`, `check-duplicate`) — extract/reuse the shared parts rather than duplicating; consumes the existing vision/review props already rendered by the camera flow.

- [ ] **Step 1: Failing tests:** (a) typing a barcode + Enter fires lookup (NOT form submit) and populates Name from the product; (b) a duplicate code renders the same dupe-warning surface the camera flow shows; (c) after a successful create, focus returns to Name (drive the submit, assert `document.activeElement`); (d) a photo dropped on the desk form renders the vision review block with unconfirmed-name styling.
- [ ] **Step 2: Implement:** barcode field `onKeyDown` Enter → `preventDefault()` + lookup; `onBlur` with non-empty value → same lookup (skip if unchanged since last lookup); submit disabled while a lookup is in flight. Focus: effect keyed on the create-settled transition ManualCreate actually has post-optimistic-commit (read the current code first; report what it is) calling `nameRef.current?.focus()`. Vision: pass `vision`/`reviewOpen`/`setReviewOpen`/`nameIsSuggested` (the camera flow's exact props — re-check names in code) into ManualCreate and render the same review block above the fields.
- [ ] **Step 3:** Gates (all capture suites green). Commit: `feat(client): ManualCreate gets the scanner-grade barcode path, focus return, vision review (#230)`

### Task 3: #231 — Bulk Delete and Tag

**Files:** Modify `client/src/pages/container-detail.tsx` (select-mode action bar — anchor: the bar rendering Move and Queue actions). Possibly a small batch-mode prop on the existing TagPicker component (find it; additive only). Test: `container-detail.bulk.test.tsx`.

**Interfaces:** consumes `useDeleteItem` (use-inventory.ts) and the container-delete equivalent (find the existing hook the single-entity delete paths use); consumes the existing tag-assign endpoint per item (find TagPicker's mutation).

- [ ] **Step 1: Failing tests:** (a) Delete over a mixed selection opens one confirm naming counts by kind ("Delete 2 items and 1 bin?"); (b) confirm runs sequential deletes; one rigged failure → outcome `Deleted 2 · 1 failed`, the failed entity stays selected, the rest gone; (c) Tag opens the picker in batch mode, applies chosen tags additively to every selected ITEM, skips containers with the "items only" note and correct counts; (d) both actions disabled while a loop runs, with progress labels.
- [ ] **Step 2: Implement Delete** per the loop constraint; recycle-bin language in the confirm (soft delete). **Step 3: Implement Tag** batch mode (additive; per-item loop; containers skipped).
- [ ] **Step 4:** Gates. Commit: `feat(client): bulk delete and tag from select mode (#231)`

### Task 4: #229a — Picker create, step-dot back, MoveItemDialog deletion

**Files:** Modify `client/src/components/inventory/destination-picker.tsx` (empty-area state ~:119-123), `client/src/pages/capture.tsx` (step dots — anchor: the static progress-dot spans), delete `client/src/components/inventory/move-item-dialog.tsx` + all imports/usages (verify unreachable first: grep usages; if any live route renders it, STOP and report). Tests: extend picker + capture suites.

- [ ] **Step 1: Failing tests:** (a) picker empty-area state renders "Create a container here"; completing the dialog fires `onPick` with the new container; (b) works when the picker is inside ManualCreate (no camera); (c) tapping a previous step dot: place → identify keeps draft+photo; identify → photo keeps the photo; (d) forward dot taps inert; (e) MoveItemDialog gone: no import remains (tsc enforces).
- [ ] **Step 2: Implement** picker create (reuse CreateContainerDialog, seed the picker's current area/property context, refresh the bin list on create). **Step 3:** step dots become buttons for PREVIOUS steps only (aria-labels; visual affordance per the page's link idiom); transitions only between the named already-valid phases. **Step 4:** delete move-item-dialog.tsx; fix imports.
- [ ] **Step 5:** Gates. Commit: `feat(client): picker dead-end create, step-dot back, drop MoveItemDialog (#229)`

### Task 5: #229b — QR resolve, alerts, recycle bulk restore

**Files:** Modify `client/src/pages/qr-redirect.tsx` (:38-41 error split, :96-101 buttons), `client/src/components/.../notification-list.tsx` (:70-77 back-link + bulk dismiss — find the actual path), the recycle-bin page (find it: `pages/recycle*.tsx`). Tests: one per surface.

- [ ] **Step 1: Failing tests:** (a) QR: a 404 renders "code doesn't exist", a 500 renders "couldn't check — try again", both offer "Scan again" (navigates to /scan) beside "Go to home"; (b) alerts: acting from a detail navigated with `{state:{from}}` renders a back link to the list; bulk dismiss loops the single endpoint with truthful outcome; (c) recycle: rows selectable, bulk Restore loops with per-entity isolation; a blocked restore whose error payload carries the ancestor renders it as a LINK (id present) or plain name (absent) — both cases tested.
- [ ] **Step 2–4: Implement** each per spec §4.2–4.4; bulk loops per the global constraint.
- [ ] **Step 5:** Gates. Commit: `feat(client): QR error split, alerts return+bulk dismiss, recycle bulk restore (#229)`

### Task 6: Visual + driven verification

**Files:** none in-repo; harness + techniques as Waves 1–2 (scratchpad `uiharness/`, `wave1-*.js`/`wave2-*.js` scripts and `*-common.js` helpers are the reference).

- [ ] Rebuild client; drive: (1) scroll restore MEASURED — long container list, open item ~30, Back → scrollTop within a few px of the recorded offset; PUSH still lands at top; (2) ManualCreate USB simulation — type barcode + Enter: lookup fires, no submit, name populates; create → focus back in Name; (3) bulk delete mixed selection with one rigged failure — truthful toast, failed stays selected; bulk tag skips containers with note; (4) picker dead-end create at a fine desk end-to-end; (5) QR 404 vs 500 fixtures; (6) recycle bulk restore + blocked-ancestor link; (7) step-dot back preserving photo (fake camera); (8) Waves 1+2 regression scripts re-run.
- [ ] Fix in the owning files, re-gate, re-drive. Commit fixes.

### Task 7: Ship

- [ ] Full gates (client + server suite once). PR `feat: close out the review — scroll restoration, USB desk path, bulk actions, dead-end fixes` closing #229 #230 #231 #232; session math (item 30 → Back lands at item 30; USB desk entry gets lookup+dupe+focus; N deletes = 1 confirm). Merge on green (poll — NO auto-merge; squash + delete branch); deploy verify by bundle hash; note remaining post-deploy checks (real-device camera from Wave 2).

---

## Self-Review

**Spec coverage:** §1→T1 (cache, POP/PUSH, sessionStorage, best-effort clamp); §2→T2 (Enter-not-submit, dupe surface, focus transition, vision props); §3→T3 (confirm counts by kind, isolation, additive tag, containers skipped); §4.1→T4 picker, §4.5→T4 dots, §4.6→T4 dialog deletion, §4.2→T5 QR, §4.3→T5 alerts, §4.4→T5 recycle; §5 testing→T1–T5 lists + T6; §6 risks→T1 (POP/PUSH is the surface, T6 measures), T3 (truthful mixed state), T4 (only valid phases).

**Placeholders:** none — every step names files, anchors, and assertions; "find it" instructions are discovery directives with the search term given, not deferred design.

**Type consistency:** `useScrollRestoration(ref)` defined T1, consumed only by root-layout in T1; no cross-task types introduced; T2 reuses existing vision prop names verified in code; T3/T5 loops reuse existing mutation hooks by name where known (`useDeleteItem`) and by discovery where not.
